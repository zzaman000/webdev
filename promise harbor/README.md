Promise Harbor

This program simulates ships docking using Promises.

Features:
- dockShip() returns a Promise with delay
- Random failure simulation
- 3 chained .then() transformations
- .catch() handles errors
- .finally() runs after each attempt

Run with:
node promise-harbor.js

Output:
Displays ship arrival logs, errors, and completion messages.
