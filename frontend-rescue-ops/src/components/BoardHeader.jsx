export default function BoardHeader() {
  return (
    <header style={{ marginBottom: 12 }}>
      <h1>Frontend Rescue Ops</h1>
      <p style={{ color: "#555" }}>
        Task Board: Add tasks, move them across columns, and keep the UI clean.
      </p>
    </header>
  );
}
