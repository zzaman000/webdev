Array Alchemy Lab

This program processes a dataset of potion objects using array methods.

Features:
- forEach prints formatted items
- filter creates subsets (strong potions, attack potions)
- map transforms data into new arrays
- report object summarizes stats

Run with:
node array-alchemy.js

Outputs:
Formatted dataset, filtered results, transformed arrays, and summary report.
