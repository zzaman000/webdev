Array Arena

Roster Type:
The roster represents monster health values.

Actions Performed:
The program can add monsters, remove monsters, update health values,
search for monsters, and shift monsters from the roster.

How It Works:
The arena runs 5 rounds. Each round randomly performs an array operation.
After the rounds, stats are calculated and displayed.

Run using:
node array-arena.js
